# Шпаргалка по RPA

## 🎯 Action Types

### `"action": "type"` - быстрый ввод
- Весь текст сразу через `send_keys()`
- Для обычных форм

### `"action": "human_input"` ✨ - посимвольный ввод
- По одному символу + триггер JS событий
- Для реактивных форм и поиска
- **Используй когда `type` не запускает поиск!**

---

## 📱 Плейсхолдеры

### Телефон
- `{phone}` → `+79161234567`
- `{phone_without_7}` → `9161234567`

### Адрес
- `{address}` → `117335, Профсоюзная 32`
- `{zipcode}` → `117335` 
- `{address_wo_zipcode}` → `Профсоюзная 32` ✨

### SMS код
- `{sms_code}` → `1234`
- `{sms_code_digit_1}` → `1`
- `{sms_code_digit_2}` → `2`
- `{sms_code_digit_3}` → `3`
- `{sms_code_digit_4}` → `4`

---

## Примеры

### Обычное поле (type)
```json
{
  "id": "enter_phone",
  "action": "type",
  "selectors": ["input[name='phone']"],
  "text": "{phone_without_7}"
}
```

### Поле с поиском (human_input)
```json
{
  "id": "search_address",
  "action": "human_input",
  "selectors": ["input.address-search"],
  "text": "{address_wo_zipcode}",
  "wait_after": 1000
}
```

**Формат адреса в БД:** `117335, Профсоюзная 32`
