# Action Types Reference

## Доступные действия (actions) в RPA конфигурациях

### 1. **`action: "type"`** - Быстрый ввод текста
**Описание**: Вводит весь текст сразу через Selenium `send_keys()`.

**Когда использовать**: Для обычных полей ввода, где не требуется триггер JS событий.

**Пример**:
```json
{
  "id": "enter_phone",
  "action": "type",
  "selectors": ["input[name='phone']"],
  "text": "{phone_without_7}",
  "wait_after": 500
}
```

---

### 2. **`action: "human_input"`** ✨ **НОВЫЙ** 
**Описание**: Посимвольный ввод текста с триггером JS событий (`input`, `change`, `keyup`) после каждого символа.

**Когда использовать**: 
- Для **реактивных форм** (поиск с автозаполнением)
- Когда сайт **отслеживает события клавиатуры**
- Когда `action: "type"` **не запускает поиск**

**Особенности**:
- Вводит каждый символ отдельно
- **БЕЗ задержек между символами** (быстрый ввод)
- Триггерит JS события после каждого символа
- Поддерживает **все те же плейсхолдеры**, что и `action: "type"`

**Пример**:
```json
{
  "id": "enter_address_search",
  "action": "human_input",
  "selectors": ["input[name='address_search']"],
  "text": "{address_wo_zipcode}",
  "wait_after": 1000
}
```

**Что происходит под капотом**:
```javascript
// Для каждого символа:
element.send_keys(char);  // Вводим символ

// Триггерим JS события
var events = ['input', 'change', 'keyup'];
events.forEach(function(eventType) {
    var event = new Event(eventType, { bubbles: true });
    element.dispatchEvent(event);
});
```

---

## Сравнение: `type` vs `human_input`

| Характеристика | `type` | `human_input` |
|---------------|--------|---------------|
| **Скорость** | ⚡ Мгновенный | ⚡ Быстрый (посимвольный) |
| **JS события** | ❌ Не триггерит | ✅ Триггерит после каждого символа |
| **Использование** | Обычные формы | Реактивные формы, поиск |
| **Плейсхолдеры** | ✅ Все | ✅ Все |
| **Задержки** | Нет | Нет |

---

## Когда использовать каждый тип

### Используйте `action: "type"` для:
- ✅ Обычных полей ввода (имя, фамилия, email)
- ✅ Форм без реактивного поиска
- ✅ Когда не нужны JS события
- ✅ Быстрого ввода больших объемов текста

### Используйте `action: "human_input"` для:
- ✅ **Поисковых полей с автозаполнением**
- ✅ **Адресных полей с подсказками**
- ✅ Когда `action: "type"` **не запускает поиск**
- ✅ Реактивных форм, отслеживающих `input` события

---

## Примеры реальных сценариев

### Сценарий 1: Поиск адреса (нужен `human_input`)

```json
{
  "steps": [
    {
      "id": "open_address_search",
      "action": "click",
      "selectors": ["button.address-search"],
      "wait_after": 500
    },
    {
      "id": "enter_address",
      "action": "human_input",
      "selectors": ["input.address-input"],
      "text": "{address_wo_zipcode}",
      "wait_after": 1000,
      "comment": "Посимвольный ввод чтобы сработал поиск адресов"
    },
    {
      "id": "select_first_result",
      "action": "click",
      "selectors": [".address-suggestion:first-child"],
      "wait_after": 500
    }
  ]
}
```

### Сценарий 2: Обычная форма регистрации (достаточно `type`)

```json
{
  "steps": [
    {
      "id": "enter_name",
      "action": "type",
      "selectors": ["input[name='first_name']"],
      "text": "Иван",
      "wait_after": 300
    },
    {
      "id": "enter_phone",
      "action": "type",
      "selectors": ["input[name='phone']"],
      "text": "{phone_without_7}",
      "wait_after": 500
    }
  ]
}
```

### Сценарий 3: Смешанное использование

```json
{
  "steps": [
    {
      "id": "enter_zipcode",
      "action": "type",
      "selectors": ["input[name='zip']"],
      "text": "{zipcode}",
      "wait_after": 300,
      "comment": "Обычное поле - используем быстрый type"
    },
    {
      "id": "search_street",
      "action": "human_input",
      "selectors": ["input[name='street_search']"],
      "text": "{address_wo_zipcode}",
      "wait_after": 1000,
      "comment": "Поиск улицы - нужен human_input для триггера событий"
    }
  ]
}
```

---

## Поддерживаемые плейсхолдеры (одинаковые для обоих action)

Оба `type` и `human_input` поддерживают:
- `{phone}`, `{phone_without_7}`
- `{address}`, `{zipcode}`, `{address_wo_zipcode}` ✨
- `{sms_code}`, `{sms_code_digit_1}`, `{sms_code_digit_2}`, `{sms_code_digit_3}`, `{sms_code_digit_4}`

---

## Отладка

### Если `action: "type"` не работает:
1. ✅ Попробуйте `action: "human_input"` вместо `type`
2. ✅ Проверьте логи - ищите "Successfully entered text (instant)" vs "human-like"
3. ✅ Убедитесь что селектор правильный

### Логи для `type`:
```
✅ Successfully entered text (instant) into: input[name='address']
```

### Логи для `human_input`:
```
⌨️ Starting character-by-character input: 25 chars
✅ Successfully entered text (human-like, 25 chars) into: input[name='address']
```

---

## Changelog

### v1.2.0 (2025-09-26)
- ✨ Добавлен новый `action: "human_input"` для посимвольного ввода с триггером JS событий
- 📝 Добавлена документация по использованию `type` vs `human_input`
- 🔧 Улучшено логирование для различения двух типов ввода

### v1.1.0 (2025-09-26)
- ✨ Добавлены плейсхолдеры `{zipcode}`, `{address_wo_zipcode}`
- 📝 Обновлена документация

### v1.0.0
- 📞 Базовый `action: "type"` с плейсхолдерами для телефона и адреса
