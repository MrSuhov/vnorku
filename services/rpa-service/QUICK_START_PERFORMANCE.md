# 🚀 Быстрый старт - Performance Aggressive Profile

## Что было сделано?

✅ Создан новый профиль `performance_aggressive` с агрессивной блокировкой медиа  
✅ Модифицированы браузер-менеджеры для поддержки CDP блокировки  
✅ Профиль установлен как дефолтный глобально  
✅ Создан тестовый скрипт для проверки  

## Как протестировать?

### Вариант 1: Простой тест (рекомендуется)

```bash
cd /Users/ss/GenAI/korzinka/services/rpa-service
python test_performance_profile.py
```

**Что проверит тест:**
- ✅ Загрузку профиля
- ✅ Создание браузера с блокировкой медиа
- ✅ Навигацию на example.com (БЕЗ изображений!)
- ✅ Проверку что контент загружается

**Что вы увидите:**
- Браузер откроется (НЕ headless - вы увидите окно)
- Страницы будут выглядеть "сломанными" - это НОРМАЛЬНО
- Изображения НЕ загружаются - это ЦЕЛЬ
- Тест завершится через ~15 секунд

### Вариант 2: Тест реального RPA флоу

Запустите авторизацию в любой ЛСД:

```bash
# Пример для авторизации
cd /Users/ss/GenAI/korzinka
# Запустите ваш обычный RPA-флоу
```

**Профиль применится автоматически** - проверьте логи:

```
🎭 Using browser profile: performance_aggressive
🚫 Applying CDP resource blocking: 16 patterns
✅ Successfully blocked 16 resource patterns via CDP
```

## Что ожидать?

### ✅ Хорошие признаки:
- Браузер открывается быстро
- Страницы загружаются в 2-5 раз быстрее
- Изображения НЕ видны (серые квадраты вместо картинок)
- Шрифты могут выглядеть иначе
- Функциональность работает (кнопки кликаются, формы заполняются)

### ⚠️ Возможные проблемы:
- **Страница выглядит полностью сломанной** → Удалите `*.css` из блокировки
- **Кнопки не кликаются** → Возможно они реализованы через изображения
- **CDP ошибки в логах** → Не критично, базовая блокировка через prefs работает

## Откат изменений (если что-то пошло не так)

Откройте файл:
```
/Users/ss/GenAI/korzinka/services/rpa-service/browser_profile_manager.py
```

Измените строку:
```python
# Было:
self.default_profile = "performance_aggressive"

# Станет:
self.default_profile = "chrome_140_working"
```

Сохраните и перезапустите сервис.

## Настройка уровня агрессивности

### Убрать блокировку CSS (если сайты ломаются):

Откройте:
```
/Users/ss/GenAI/korzinka/services/rpa-service/browser_profiles/performance_aggressive.json
```

Найдите массив `cdp_commands.block_urls_patterns` и удалите строку:
```json
"*.css"
```

### Убрать блокировку шрифтов (если текст не читаем):

Удалите из того же массива:
```json
"*.woff",
"*.woff2",
"*.ttf",
"*.eot",
"*.otf"
```

## Мониторинг работы

Смотрите логи RPA-сервиса:
```bash
tail -f /Users/ss/GenAI/korzinka/logs/rpa_service.log
```

Ищите строки:
```
🎭 Using browser profile: performance_aggressive
🚫 Applying CDP resource blocking: X patterns
✅ Successfully blocked X resource patterns via CDP
```

## Следующие шаги

1. **Запустите тест** → `python test_performance_profile.py`
2. **Проверьте что браузер открывается БЕЗ изображений**
3. **Запустите реальный RPA-флоу** (авторизация в ЛСД)
4. **Замерьте время выполнения** (должно быть быстрее!)
5. **Если есть проблемы** → откатите или настройте профиль

## Помощь

Если возникли вопросы:
1. Проверьте логи в `/Users/ss/GenAI/korzinka/logs/`
2. Прочитайте полную документацию в `PERFORMANCE_PROFILE_README.md`
3. Попробуйте откатить изменения (см. выше)

---

**Готово к тестированию! 🚀**
