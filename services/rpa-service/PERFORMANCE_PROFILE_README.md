# Performance Aggressive Profile - Документация

## Описание

**Performance Aggressive** - это браузерный профиль для максимально быстрого выполнения RPA-задач с агрессивной блокировкой всех медиа-ресурсов.

## Что блокируется?

### Через Chrome Preferences (prefs):
- ✅ Изображения (JPG, PNG, GIF, WebP и т.д.)
- ✅ Анимации
- ✅ Плагины
- ✅ Всплывающие окна (popups)
- ✅ Геолокация
- ✅ Уведомления
- ✅ Медиа-потоки (микрофон, камера)
- ✅ Автоматические загрузки

### Через Chrome Arguments:
- ✅ GPU-ускорение
- ✅ Плавная прокрутка
- ✅ WebGL/WebGL2
- ✅ Аудио
- ✅ Удаленные шрифты
- ✅ Фоновая сетевая активность
- ✅ Синхронизация
- ✅ Автообновление компонентов

### Через CDP (Chrome DevTools Protocol):
- ✅ Изображения: `*.jpg`, `*.jpeg`, `*.png`, `*.gif`, `*.webp`, `*.svg`, `*.ico`
- ✅ Шрифты: `*.woff`, `*.woff2`, `*.ttf`, `*.eot`, `*.otf`
- ✅ Видео: `*.mp4`, `*.webm`, `*.ogg`
- ✅ Аудио: `*.mp3`, `*.wav`
- ✅ Стили: `*.css` (ОПЦИОНАЛЬНО - может сломать некоторые сайты)

## Преимущества

✅ **Скорость**: Загрузка страниц в 2-5 раз быстрее  
✅ **Ресурсы**: Снижение потребления RAM на 40-60%  
✅ **Трафик**: Экономия интернет-трафика до 80%  
✅ **Стабильность**: Меньше элементов = меньше точек отказа  

## Недостатки

⚠️ **Визуальный анализ затруднен**: Страницы выглядят "сломанными"  
⚠️ **Некоторые сайты могут не работать**: Если кнопки реализованы через изображения  
⚠️ **Блокировка CSS может сломать layout**: Рекомендуется убрать `*.css` из блокировки если возникают проблемы  

## Использование

### Глобальное включение (по умолчанию)

Профиль **performance_aggressive** установлен как дефолтный в `browser_profile_manager.py`:

```python
self.default_profile = "performance_aggressive"
```

Все RPA-задачи автоматически используют этот профиль.

### Ручное использование

```python
from selenium_browser_manager_fixed import UndetectedBrowserManager

browser_manager = UndetectedBrowserManager(headless=False, debug=True)
driver = await browser_manager.create_undetected_browser(
    profile_name='performance_aggressive'
)
```

### Использование другого профиля

Если нужен стандартный профиль БЕЗ блокировки медиа:

```python
driver = await browser_manager.create_undetected_browser(
    profile_name='chrome_140_working'  # Стандартный профиль
)
```

## Тестирование

Запустите тестовый скрипт:

```bash
cd /Users/ss/GenAI/korzinka/services/rpa-service
python test_performance_profile.py
```

Тест проверит:
- ✅ Загрузку профиля
- ✅ Создание браузера
- ✅ Навигацию на тестовые страницы
- ✅ Блокировку медиа-ресурсов

## Настройка профиля

Файл профиля: `/Users/ss/GenAI/korzinka/services/rpa-service/browser_profiles/performance_aggressive.json`

### Отключение блокировки CSS (если сайты ломаются)

Откройте `performance_aggressive.json` и удалите `*.css` из массива `cdp_commands.block_urls_patterns`:

```json
"block_urls_patterns": [
  "*.jpg",
  "*.jpeg",
  "*.png",
  "*.gif",
  // ... остальные паттерны
  // "*.css"  <- закомментируйте или удалите эту строку
]
```

### Добавление дополнительных блокировок

Можно добавить любые паттерны URL в массив `block_urls_patterns`:

```json
"block_urls_patterns": [
  "*.jpg",
  "*analytics.js",      // Блокировка аналитики
  "*facebook.com/*",    // Блокировка Facebook виджетов
  "*doubleclick.net/*"  // Блокировка рекламы
]
```

## Когда использовать?

✅ **Используйте performance_aggressive для:**
- Массовая авторизация пользователей
- Поиск товаров в ЛСД
- Сбор данных без визуального анализа
- Любые задачи где важна скорость

❌ **НЕ используйте для:**
- Отладки визуальных проблем (сайты будут выглядеть "сломанными")
- Сайтов где кнопки реализованы через изображения
- Задач требующих CSS для корректной работы

## Возврат к стандартному профилю

Если performance_aggressive вызывает проблемы, откройте `browser_profile_manager.py` и измените:

```python
self.default_profile = "chrome_140_working"  # Стандартный профиль
```

## Логи

При создании браузера с performance_aggressive вы увидите:

```
🚀 Launching FIXED undetected Chrome...
🎭 Using browser profile: performance_aggressive
🚫 Applying CDP resource blocking: 16 patterns
✅ Successfully blocked 16 resource patterns via CDP
🚫 Blocked patterns: *.jpg, *.jpeg, *.png, *.gif, *.webp...
```

## Troubleshooting

### Проблема: Страница выглядит сломанной
**Решение**: Это нормально - изображения и стили заблокированы. Используйте стандартный профиль для визуальной отладки.

### Проблема: Кнопки не кликаются
**Решение**: Возможно кнопки реализованы через изображения. Попробуйте стандартный профиль или удалите блокировку изображений.

### Проблема: CDP блокировка не работает
**Решение**: Это не критично - базовая блокировка через prefs и arguments всё равно работает.

### Проблема: Сайт не загружается вообще
**Решение**: Удалите `*.css` из `block_urls_patterns` в профиле.

## История изменений

- **2025-10-03**: Создан агрессивный performance профиль с блокировкой медиа
- **2025-10-03**: Добавлена поддержка CDP блокировки в browser managers
- **2025-10-03**: Установлен как дефолтный профиль глобально

## Контакты

Вопросы и предложения: создайте issue или обратитесь к разработчикам проекта.
