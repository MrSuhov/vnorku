# Чеклист тестирования: Контекстная блокировка медиа

## Дата: 2025-01-03

---

## 1. Тест авторизации с QR-кодом

**ЛСД:** Яндекс Лавка (или другая с QR-авторизацией)

**Шаги:**
1. Запустить RPA-service
2. Инициировать авторизацию пользователя в ЛСД
3. Дождаться появления QR-кода в Telegram

**Ожидаемый результат:**
- ✅ QR-код отображается корректно
- ✅ QR-код можно отсканировать
- ✅ Авторизация проходит успешно
- ✅ В логах: `🖼️ NORMAL MODE: Media blocking DISABLED`

**Команда для проверки логов:**
```bash
tail -f /Users/ss/GenAI/korzinka/logs/rpa-service.log | grep "NORMAL MODE"
```

---

## 2. Тест модальных окон (поп-апов)

**ЛСД:** Ашан (модалка подтверждения возраста)

**Шаги:**
1. Выполнить авторизацию в Ашане
2. Запустить поиск товара
3. Проверить, появляется ли модальное окно с возрастом

**Ожидаемый результат:**
- ✅ Модальное окно появляется
- ✅ Кнопки в модальном окне кликабельны
- ✅ После закрытия модалки поиск продолжается
- ✅ Товары находятся корректно

**Команда для проверки логов:**
```bash
tail -f /Users/ss/GenAI/korzinka/logs/rpa-service.log | grep "Modal"
```

---

## 3. Тест поиска товаров с блокировкой медиа

**ЛСД:** Любая (например, Лента)

**Шаги:**
1. Создать заказ с 5-10 товарами
2. Запустить поиск товаров в ЛСД
3. Проверить результаты поиска

**Ожидаемый результат:**
- ✅ Поиск выполняется быстро
- ✅ Товары находятся корректно
- ✅ Цены извлекаются правильно
- ✅ Единицы измерения определяются верно
- ✅ В логах: `🚫 PERFORMANCE MODE: Media blocking ENABLED`

**Команда для проверки логов:**
```bash
tail -f /Users/ss/GenAI/korzinka/logs/rpa-service.log | grep "PERFORMANCE MODE"
```

**Измерение времени:**
```bash
# Засечь время начала и конца поиска в логах
tail -f /Users/ss/GenAI/korzinka/logs/rpa-service.log | grep "Starting product search\|search completed"
```

---

## 4. Тест производительности (опционально)

**Цель:** Сравнить скорость поиска с блокировкой и без

**Методика:**
1. Временно изменить `block_media=True` на `block_media=False` в строке 627 main.py
2. Выполнить поиск 10 товаров, засечь время
3. Вернуть `block_media=True`
4. Выполнить тот же поиск, засечь время
5. Сравнить результаты

**Ожидаемый результат:**
- ✅ С `block_media=True` поиск быстрее на 20-40%
- ✅ Корректность результатов одинаковая в обоих случаях

---

## 5. Проверка логирования

**Команда:**
```bash
grep -E "NORMAL MODE|PERFORMANCE MODE|Popups: ALLOWED" /Users/ss/GenAI/korzinka/logs/rpa-service.log | tail -20
```

**Ожидаемый результат:**
- ✅ При авторизации: `NORMAL MODE` + `Popups: ALLOWED`
- ✅ При поиске: `PERFORMANCE MODE` + `Popups: ALLOWED`

---

## Критерии успешного прохождения

**MUST HAVE (обязательно):**
- [x] QR-коды отображаются при авторизации
- [x] Модальные окна работают
- [x] Поиск товаров выполняется корректно
- [x] Логирование показывает правильный режим

**NICE TO HAVE (желательно):**
- [ ] Производительность поиска улучшилась
- [ ] Нет ошибок antibot защиты
- [ ] Трафик при поиске снизился

---

## Возможные проблемы и решения

### Проблема: QR-код не отображается
**Решение:**
1. Проверить логи - должно быть `NORMAL MODE`
2. Проверить, что в строке 3110 main.py НЕТ `block_media=True`
3. Перезапустить RPA-service

### Проблема: Модальное окно не появляется
**Решение:**
1. Проверить логи на наличие `Popups: ALLOWED`
2. Проверить конфигурацию `post_search_modals` в search_config_rpa
3. Добавить задержку после поиска

### Проблема: Поиск стал медленнее
**Решение:**
1. Проверить, что в строке 627 main.py ЕСТЬ `block_media=True`
2. Проверить логи - должно быть `PERFORMANCE MODE`
3. Возможно, сайт детектит блокировку медиа - попробовать убрать

---

## Контакты для вопросов
- Логи: `/Users/ss/GenAI/korzinka/logs/rpa-service.log`
- Код: `/Users/ss/GenAI/korzinka/services/rpa-service/`
- Документация: `MEDIA_BLOCKING_CHANGES.md`
