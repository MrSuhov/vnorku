# Исследование проблемы авторизации через CDP для Перекрёстка

## Дата исследования
- **Первая сессия:** 19 октября 2025, 01:00 - 02:00
- **Вторая сессия:** 19 октября 2025, 21:00 - 21:30
- **Третья сессия:** 19 октября 2025, 21:45 - 22:20 (Persistent Profile Investigation)

## Проблема
При использовании CDP режима с инжектом cookies из JSON файла пользователь оказывается **разлогиненным** на сайте Перекрёстка (www.perekrestok.ru), несмотря на то что:
- Все cookies инжектируются корректно (34 шт)
- localStorage инжектируется корректно (17 элементов)
- sessionStorage инжектируется корректно (1 элемент)

## Архитектура авторизации Перекрёстка

### Компоненты авторизации
1. **Cookie `session`** - содержит JSON с токенами:
   ```json
   {
     "accessToken": "JWT токен",
     "refreshToken": "JWT токен для обновления",
     "accessTokenExpiredAt": 1760824111000,
     "refreshTokenExpiredAt": 2076356551224,
     "device": {"uuid": "..."}
   }
   ```

2. **localStorage** содержит:
   - `UserId` - ID пользователя
   - `server_token` - JWT токен сервера
   - `session_token_timestamp` - время создания токена
   - `refresh_token_trigger` - триггер для обновления
   - `publicKey`, `privateKey` - ключи шифрования
   - `encryptedUniqueWebId` - уникальный ID
   - `tpi` - ID пользователя в системе

3. **Auth сервер**: `id.x5.ru` (Keycloak)

### Жизненный цикл токенов
- **accessToken**: живёт ~6 минут (360 секунд)
- **refreshToken**: живёт до 2076 года (практически вечный)
- При истечении accessToken приложение **должно** автоматически использовать refreshToken для получения нового accessToken

## Проведённые тесты

### Тест 1: Базовый CDP инжект (failed ❌)
**Подход:**
```
1. Инжект cookies через CDP
2. Навигация на base_url
3. Инжект localStorage/sessionStorage после загрузки
```

**Результат:** 
- ❌ Пользователь разлогинен
- ❌ В localStorage появляется `x5idRedirectUri=/login` (маркер незалогиненного пользователя)

**Вывод:** Приложение инициализируется БЕЗ токенов в storage, поэтому считает пользователя неавторизованным.

---

### Тест 2: Reload после инжекта storage (failed ❌)
**Подход:**
```
1. Инжект cookies
2. Навигация на base_url
3. Инжект localStorage/sessionStorage
4. driver.refresh() - обновление страницы
5. await asyncio.sleep(5) - ожидание refresh токена
```

**Результат:** 
- ❌ Пользователь разлогинен
- ❌ Приложение не делает refresh токена

**Вывод:** Простое обновление страницы не помогает - приложение уже "решило" что пользователь не авторизован.

---

### Тест 3: Двойная навигация (failed ❌)
**Подход:**
```
1. Инжект cookies
2. Первая навигация на base_url (для доступа к localStorage)
3. Инжект localStorage/sessionStorage
4. Вторая навигация на base_url (приложение должно увидеть storage)
```

**Результат:**
- ❌ Пользователь разлогинен
- ❌ `x5idRedirectUri=/login` всё равно появляется

**Вывод:** Даже с инжектом ДО полной загрузки приложения - не работает.

---

### Тест 4: Инжект на origin перед финальной навигацией (failed ❌)
**Подход:**
```
1. Инжект cookies
2. Навигация на origin (https://www.perekrestok.ru)
3. Инжект localStorage/sessionStorage
4. Финальная навигация на base_url (/delivery/)
```

**Результат:**
- ❌ Пользователь разлогинен
- ❌ Приложение не понимает что пользователь авторизован

**Вывод:** Порядок операций не решает проблему.

---

## Ключевые наблюдения

### 1. Safari vs CDP - в чём разница?

**Safari (работает ✅):**
- Экспортированные данные содержат **истёкший accessToken** (истёк 34+ минуты назад)
- Но в Safari пользователь **остаётся залогиненным**
- **Гипотеза:** Safari при открытии сайта каким-то образом триггерит автоматический refresh токена

**CDP (не работает ❌):**
- Инжектим **те же самые данные** (истёкший accessToken + валидный refreshToken)
- Пользователь оказывается **разлогиненным**
- **Проблема:** Приложение НЕ делает автоматический refresh

### 2. Отсутствие запросов к auth серверу

**Критичное наблюдение:**
- В Network вкладке DevTools **НЕТ запросов** к `id.x5.ru`
- Это значит приложение **вообще не пытается** обновить токен
- Приложение просто считает пользователя неавторизованным

### 3. Маркер разлогина

В localStorage **появляется** ключ:
```
x5idRedirectUri = "/login"
```

Этот ключ:
- **НЕ присутствует** в экспортированных данных из Safari
- **Создаётся самим приложением** когда оно решает что пользователь не авторизован
- Указывает куда редиректнуть пользователя для логина

### 4. Состояние accessToken

**JWT payload accessToken:**
```json
{
  "exp": 1760824111,  // Истёк 19.10.2025 00:48:31
  "iat": 1760823751,
  "x5id": "14650871",
  "preferred_username": "79262041000",
  "email": "mrsuhov@gmail.com"
}
```

**На момент тестов (02:00):**
- Token истёк **71 минуту назад**
- RefreshToken **валиден** до 2076 года
- Но приложение **не использует** refreshToken

## Возможные причины проблемы

### Гипотеза 1: Порядок инициализации
Приложение Перекрёстка, возможно, имеет жёсткий порядок инициализации:
1. При загрузке проверяет localStorage
2. Если находит токены - считает пользователя авторизованным
3. Если НЕ находит - помечает как неавторизованного
4. Это решение **кешируется** и больше не меняется

**Проблема:** Мы инжектим storage ПОСЛЕ того как приложение уже приняло решение.

### Гипотеза 2: Дополнительные проверки
Приложение может проверять:
- **Browser fingerprint** - отпечаток браузера должен совпадать с оригинальным
- **Device UUID** - должен совпадать с тем что в токене (`25ba8aa1-0f77-4aeb-8186-84bb59d90233`)
- **Session binding** - токены могут быть привязаны к конкретной сессии Chrome
- **Encryption keys** - `publicKey`/`privateKey` в localStorage должны соответствовать токенам

### Гипотеза 3: Persistent profile vs CDP
**Persistent profile (не работает с истёкшими токенами):**
- Cookies в SQLite базе Chrome
- Зашифрованы через Chrome Keychain
- Обновлялись 16 октября (3 дня назад)
- AccessToken давно истёк

**CDP режим (не работает даже со свежим экспортом):**
- Cookies инжектятся "на лету"
- Может не хватать какого-то контекста который есть в persistent profile
- Или наоборот - persistent profile имеет старый "плохой" контекст

### Гипотеза 4: Service Workers / IndexedDB
Возможно Перекрёсток использует:
- **Service Workers** для кеширования auth состояния
- **IndexedDB** для хранения дополнительных данных
- **Web Crypto API** для шифрования/дешифрования токенов

И мы **НЕ экспортируем/инжектим** эти данные, только cookies + localStorage.

## Что НЕ решило проблему

1. ❌ Увеличение времени ожидания после инжекта
2. ❌ Reload страницы после инжекта storage
3. ❌ Двойная навигация (pre-load + final load)
4. ❌ Изменение порядка инжекта (cookies → storage → navigate)
5. ❌ Инжект на origin вместо base_url
6. ❌ Использование свежих cookies из Safari (истёкший accessToken везде)

## Следующие шаги для исследования

### Вариант 1: Полная авторизация через RPA (рекомендуется)
**Подход:**
1. Использовать persistent profile режим
2. Выполнить полную авторизацию через `/auth/initiate`
3. Пройти RPA flow с SMS кодом
4. Сохранить результат в persistent profile (SQLite)
5. **Предположение:** Свежая авторизация создаст все нужные контексты

**Плюсы:**
- ✅ Получим свежий accessToken (не истёкший)
- ✅ Все контексты будут созданы правильно
- ✅ Browser fingerprint будет корректным

**Минусы:**
- ❌ Нужна авторизация для каждого пользователя
- ❌ AccessToken всё равно истечёт через 6 минут

### Вариант 2: Автоматический refresh токена перед инжектом
**Подход:**
1. Читаем JSON файл
2. Проверяем не истёк ли accessToken
3. Если истёк - делаем API запрос к `id.x5.ru` с refreshToken
4. Получаем новый accessToken
5. Обновляем JSON
6. Инжектим УЖЕ свежий токен

**Плюсы:**
- ✅ Всегда инжектим валидный accessToken
- ✅ Можем использовать один раз авторизованную сессию долго

**Минусы:**
- ❌ Нужно реверсить API Перекрёстка
- ❌ Может требовать дополнительные параметры (device UUID, headers)

### Вариант 3: Экспорт из Safari в persistent profile
**Подход:**
1. Экспортируем cookies из Safari
2. Конвертируем в формат SQLite базы Chrome
3. Записываем в persistent profile
4. Используем persistent profile для поиска

**Плюсы:**
- ✅ Используем "родной" механизм Chrome
- ✅ Cookies будут правильно зашифрованы

**Минусы:**
- ❌ Сложная конвертация форматов
- ❌ Safari использует другое шифрование

### Вариант 4: Изучить Service Workers и IndexedDB
**Подход:**
1. В Safari открыть DevTools
2. Проверить Application → Service Workers
3. Проверить Application → IndexedDB
4. Экспортировать эти данные тоже
5. Инжектить в CDP браузер

**Плюсы:**
- ✅ Может найти недостающее звено

**Минусы:**
- ❌ Сложная реализация
- ❌ Может не быть критичным

## Рекомендации

### Краткосрочное решение (для MVP)
**Использовать persistent profile с RPA авторизацией:**
```python
# 1. Переключить Перекрёсток на persistent profile
UPDATE lsd_configs 
SET rpa_config = jsonb_set(rpa_config, '{cdp_enabled}', 'false') 
WHERE name = 'perek';

# 2. Выполнить авторизацию через /auth/initiate
# 3. Использовать persistent profile для поиска
```

**Плюсы:**
- ✅ Работает сейчас
- ✅ Не требует дополнительных исследований

**Минусы:**
- ❌ AccessToken истечёт через 6 минут
- ❌ Нужен механизм refresh через RPA

### Долгосрочное решение
**Реализовать автоматический refresh токена:**
1. Реверсить API endpoint для token refresh
2. Перед каждым использованием проверять expiry
3. Автоматически обновлять через API
4. Сохранять обновлённые токены обратно в JSON

## Технические детали

### Структура cookie `session`
```json
{
  "name": "session",
  "value": "j:{...JSON...}",
  "domain": "www.perekrestok.ru",
  "path": "/",
  "expiry": 1760824111,
  "secure": true,
  "httpOnly": false,
  "sameSite": "Lax"
}
```

### Структура localStorage (ключевые элементы)
```javascript
{
  "UserId": "14650871",
  "server_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "session_token_timestamp": "1759835290282",
  "refresh_token_trigger": "0.576526135...",
  "publicKey": "30819f300d06092a864886f70d010101...",
  "privateKey": "308202750201003082...",
  "encryptedUniqueWebId": "6222121ba17...",
  "tpi": "14650871"
}
```

### Endpoint для token refresh (предположительно)
```
POST https://id.x5.ru/auth/realms/ssox5id/protocol/openid-connect/token
Content-Type: application/x-www-form-urlencoded

grant_type=refresh_token
&client_id=tcx_web
&refresh_token={refreshToken}
```

## Выводы

1. **CDP инжект с истёкшими токенами не работает** для Перекрёстка
2. **Приложение НЕ делает автоматический refresh** при инжекте через CDP
3. **Safari работает** даже с истёкшими токенами (причины неясны)
4. **Persistent profile** требует свежей авторизации через RPA
5. **Необходимо** либо делать refresh API вручную, либо использовать RPA авторизацию

---

## Вторая сессия исследования (19.10.2025 21:00 - 21:30)

### Тест 5: Исправление порядка инжекта localStorage (failed ❌)
**Подход:**
```
1. Инжект cookies через CDP
2. Навигация на origin (https://www.perekrestok.ru)
3. Инжект localStorage/sessionStorage
4. Навигация на base_url (/delivery/)
```

**Гипотеза:**
Приложение Перекрёстка проверяет localStorage **при инициализации** (сразу при загрузке страницы). Если инжектировать localStorage ПОСЛЕ загрузки base_url, приложение уже приняло решение что пользователь неавторизован.

**Реализация:**
Изменена последовательность в `services/rpa-service/main.py`:
- Функция `perform_product_search_with_cdp_cookies` (строки 947-985)
- Функция `browse_open` (строки 4237-4265)

**Код изменений:**
```python
# БЫЛО:
driver.get(base_url)
inject_local_storage(...)

# СТАЛО:
driver.get(origin_url)  # https://www.perekrestok.ru
inject_local_storage(...)
driver.get(base_url)    # https://www.perekrestok.ru/delivery/
```

**Результат:**
- ✅ localStorage инжектируется корректно (17 items)
- ✅ Порядок правильный (origin → storage → base_url)
- ❌ Пользователь всё равно разлогинен

**Вывод:** Порядок инжекта не решает проблему.

---

### Тест 6: Обновление localStorage актуальными данными из Safari (failed ❌)
**Подход:**
```
1. Экспорт актуального localStorage из авторизованного Safari
2. Обновление JSON файла с cookies
3. Повторный тест с полным набором ключей
```

**Обнаруженная проблема:**
В JSON файле `cookies/perek/99777490_perek.json` **отсутствовали критические ключи**:

Было 16 ключей, отсутствовали:
- `server_token` - JWT токен сервера (критичный!)
- `session_token_timestamp` - время создания токена (критичный!)
- `is_cannot_get_auth_tokens` - флаг проблем с токенами
- `_ym72082048_il` - Yandex Metrika
- `_ym72082048_lastHit` - Yandex Metrika
- `previousProduct` - последний просмотренный товар
- `x5idRedirectUri` - кеш редиректа

**Также обнаружено:**
- `UserId` был `"0000000000000000"` вместо `"14650871"`
- Ключ `x5idRedirectUri: "/login"` присутствует ДАЖЕ в авторизованном Safari (это просто кеш, не причина разлогина)

**Действия:**
1. Экспортирован полный localStorage из Safari (23 ключа)
2. Обновлён JSON файл cookies
3. Проверены критические ключи:
   - ✅ `server_token`: присутствует (JWT токен)
   - ✅ `session_token_timestamp`: 1760913279136
   - ✅ `UserId`: "14650871"
   - ✅ `tpi`: "14650871"
   - ✅ `is_cannot_get_auth_tokens`: "false"

**Скрипт экспорта из Safari:**
```javascript
// Выполнить в Safari DevTools Console на авторизованной странице
(function() {
    const data = { localStorage: {}, sessionStorage: {} };
    for (let i = 0; i < localStorage.length; i++) {
        const key = localStorage.key(i);
        data.localStorage[key] = localStorage.getItem(key);
    }
    for (let i = 0; i < sessionStorage.length; i++) {
        const key = sessionStorage.key(i);
        data.sessionStorage[key] = sessionStorage.getItem(key);
    }
    console.log(JSON.stringify(data, null, 2));
})();
```

**Результат тестирования:**
- ✅ Все 23 ключа localStorage инжектированы
- ✅ sessionStorage обновлён (1 ключ)
- ✅ Порядок инжекта правильный
- ✅ Валидность токенов проверена (accessToken валиден 458 минут)
- ❌ Пользователь **всё равно разлогинен**

**Вывод:** Даже полный набор localStorage из авторизованного Safari не решает проблему.

---

### Анализ проблемы

#### Что точно работает:
1. ✅ **Cookies инжектируются корректно** (39 cookies, включая `session` с валидным accessToken)
2. ✅ **localStorage инжектируется полностью** (все 23 ключа из Safari)
3. ✅ **sessionStorage инжектируется** (1 ключ)
4. ✅ **Порядок инжекта правильный** (origin → storage → base_url)
5. ✅ **Токены валидны** (accessToken истекает через 458 минут, refreshToken через 179 дней)

#### Что НЕ работает:
❌ Пользователь остаётся **разлогиненным** несмотря на всё вышеперечисленное

#### Возможные причины (гипотезы):

**Гипотеза 1: Дополнительные хранилища**
Перекрёсток может использовать:
- **IndexedDB** - не экспортируется/не инжектируется
- **Service Workers** - может кешировать auth состояние
- **Cache API** - может хранить auth данные
- **Web Crypto API storage** - недоступно через CDP

**Гипотеза 2: API проверка токенов**
Приложение может делать:
- **Синхронный API запрос** при загрузке к `id.x5.ru` для валидации токена
- CDP может блокировать/изменять этот запрос
- Или запрос не срабатывает из-за отсутствия каких-то headers

**Гипотеза 3: Browser Fingerprint**
Перекрёсток может проверять:
- **Fingerprint браузера** должен совпадать с тем, что в токене
- CDP браузер имеет другой fingerprint чем Safari
- Device UUID в токене (`f712d007-58e6-45bd-a8d9-7f799694d2d4`) не соответствует CDP браузеру

**Гипотеза 4: Anti-automation детекция**
Сайт может детектировать:
- CDP режим (флаг `navigator.webdriver`)
- Отсутствие "естественной" истории браузера
- Слишком быструю загрузку без задержек пользователя

**Гипотеза 5: Cryptographic verification**
Ключи `publicKey` и `privateKey` в localStorage могут использоваться для:
- Подписи запросов к API
- Дешифровки данных от сервера
- Проверки integrity данных
- CDP может не иметь доступа к нужным crypto API

#### Что точно НЕ является проблемой:

1. ✅ **НЕ порядок инжекта** - исправлен, не помогло
2. ✅ **НЕ отсутствие ключей localStorage** - все 23 ключа есть, не помогло
3. ✅ **НЕ истёкшие токены** - токены валидны, не помогло
4. ✅ **НЕ `x5idRedirectUri: "/login"`** - этот ключ есть даже в авторизованном Safari (просто кеш)
5. ✅ **НЕ неправильный UserId** - исправлен на "14650871", не помогло

---

## Обновлённые выводы

1. **CDP инжект НЕ работает** для Перекрёстка даже при идеальных условиях:
   - ✅ Правильный порядок инжекта
   - ✅ Полный набор localStorage из Safari
   - ✅ Валидные токены
   - ✅ Все cookies присутствуют

2. **Приложение Перекрёстка имеет сложную систему авторизации** которая:
   - Не может быть воспроизведена только через cookies + localStorage + sessionStorage
   - Вероятно использует IndexedDB, Service Workers, или другие механизмы
   - Может проверять browser fingerprint или делать API запросы для валидации

3. **Safari работает с теми же данными** что мы инжектируем:
   - Это доказывает что данные корректны
   - Но Safari имеет дополнительный контекст (IndexedDB, Service Workers, history, etc)
   - CDP браузер не имеет этого контекста

4. **Единственное надёжное решение для Перекрёстка:**
   - ✅ **Persistent profile** с полной RPA авторизацией через SMS
   - Авторизация один раз сохраняется в SQLite базе Chrome
   - Все дополнительные хранилища (IndexedDB, Service Workers) сохраняются автоматически
   - Browser fingerprint остаётся стабильным

---

## Рекомендации

### Для Перекрёстка (MVP):
**Использовать persistent profile режим:**
```sql
UPDATE lsd_configs
SET rpa_config = rpa_config::jsonb || '{"cdp_enabled": false, "use_persistent_profile": true}'::jsonb
WHERE name = 'perek';
```

**Авторизация:**
1. Пользователь выполняет `/auth perek` в боте
2. RPA flow открывает браузер с persistent profile
3. Пользователь проходит SMS авторизацию
4. Cookies сохраняются в SQLite базу профиля
5. IndexedDB, Service Workers сохраняются автоматически
6. Все последующие поиски используют этот профиль

**Плюсы:**
- ✅ Работает надёжно (как в обычном браузере)
- ✅ Все механизмы авторизации сохраняются
- ✅ Browser fingerprint стабильный
- ✅ Не нужен refresh токенов

**Минусы:**
- ⚠️  Требует RPA авторизацию для каждого пользователя
- ⚠️  Занимает место на диске (~50-100MB на профиль)

### Для других LSD:
**CDP режим работает для:**
- ✅ Самокат
- ✅ Ozon
- ✅ Лента
- ✅ Ашан
- ✅ Магнит

**Эти ЛСД имеют более простую авторизацию:**
- Достаточно cookies + localStorage
- Не используют сложные механизмы проверки
- Работают через CDP инжект

---

## Тест 7: Исследование persistent profile (Третья сессия)

**Дата:** 19 октября 2025, 21:45 - 22:20
**Цель:** Выяснить почему авторизация "слетает" через несколько часов после RPA авторизации в persistent profile режиме.

### Контекст
После провала всех CDP тестов, переключились на **persistent profile режим** (`cdp_enabled=false`).
- RPA авторизация проходит успешно
- Через несколько часов авторизация пропадает
- Нужно понять причину

### Проверенные гипотезы

#### Гипотеза 1: Браузер закрывается слишком быстро ❌
**Идея:** `driver.quit()` не даёт времени на flush данных на диск

**Тест:**
- Добавили `time.sleep(2)` перед `quit()`
- **Результат:** НЕ помогло

**Вывод:** Данные пишутся мгновенно, задержка не решает проблему. Event-based подход вместо time-based.

#### Гипотеза 2: UserId = "0000000000000000" означает разлогин ✅
**Идея:** Видели что `UserId` всегда "0000000000000000" и думали что это означает отсутствие авторизации

**Тест:** Добавили диагностику localStorage СРАЗУ после успешной авторизации (до закрытия браузера)

```python
# services/rpa-service/main.py:3351-3371
logger.info(f"🔍 localStorage IMMEDIATELY after auth:")
user_id = driver.execute_script("return localStorage.getItem('UserId');")
tpi = driver.execute_script("return localStorage.getItem('tpi');")
server_token = driver.execute_script("return localStorage.getItem('server_token');")
```

**Результат:**
```
22:16:56 INFO __main__ 🔍 localStorage IMMEDIATELY after auth:
22:16:56 INFO __main__    UserId: 0000000000000000
22:16:56 INFO __main__    tpi: 14650871
22:16:56 INFO __main__    server_token: present (312 chars)
22:16:56 WARNING __main__ ⚠️ UserId is DEFAULT VALUE right after auth!
```

**Вывод:** ✅ **UserId = "0000000000000000" это НЕ индикатор разлогина!**

Перекрёсток не использует это поле. Реальные индикаторы авторизации:
- ✅ Cookie `session` с accessToken/refreshToken
- ✅ `server_token` в localStorage (312 chars)
- ✅ `tpi` = 14650871 (реальный ID пользователя)

#### Гипотеза 3: Persistent profile не работает между запусками ✅
**Идея:** Может данные записываются, но не загружаются при следующем запуске?

**Тест:**
1. Выполнили `/auth perek` в 22:16
2. RPA авторизация прошла успешно
3. Браузер закрылся (persistent profile сохранён)
4. СРАЗУ открыли `/browse/open` в 22:17
5. Проверили UI - нажали кнопку "Профиль"

**Результат:** ✅ **Браузер открылся АВТОРИЗОВАННЫМ!**
- Виден профиль пользователя
- Виден адрес доставки
- Кнопка "Профиль" показывает профиль (не просьбу войти)

**Вывод:** ✅ **Persistent profile РАБОТАЕТ и сохраняется между запусками браузера!**

### Текущие выводы

**✅ Работает:**
- RPA авторизация через persistent profile
- Сохранение авторизации между запусками (проверено сразу после авторизации)
- Persistent profile корректно записывается и читается

**❓ Неизвестно:**
- **Почему авторизация слетает через несколько часов?**
- Как именно происходит "слёт"?

### План мониторинга (в процессе)

**Время авторизации:** 22:16:56

**График проверок:**
- ✅ **22:17** - СРАЗУ после авторизации → **АВТОРИЗОВАН** ✅
- 🕐 **23:17** - Через 1 час → ❓ (запланирована)
- 🕐 **00:17** - Через 2 часа → ❓ (запланирована)
- 🕐 **02:17** - Через 4 часа → ❓ (запланирована)
- 🕐 **06:17** - Через 8 часов (время истечения accessToken) → ❓ (запланирована)

**Что проверять:**
```javascript
// В DevTools Console
console.log('UserId:', localStorage.getItem('UserId'));
console.log('tpi:', localStorage.getItem('tpi'));
console.log('server_token:', localStorage.getItem('server_token') ? 'present' : 'MISSING');

const sessionCookie = document.cookie.split('; ').find(c => c.startsWith('session='));
if (sessionCookie) {
    const data = JSON.parse(decodeURIComponent(sessionCookie.split('=')[1]).substring(2));
    console.log('accessTokenExpiredAt:', new Date(data.accessTokenExpiredAt));
    console.log('accessToken expired?', Date.now() > data.accessTokenExpiredAt);
}

// ГЛАВНОЕ: нажать кнопку "Профиль" и проверить UI
```

### Возможные сценарии "слёта"

**Сценарий 1: accessToken истекает (8 часов), refreshToken не обновляется**
- Service Workers не работают при закрытом браузере
- Auto-refresh не срабатывает
- При следующем открытии accessToken уже expired
- **Решение:** Browser Keep-Alive Pool

**Сценарий 2: Cookie session удаляется**
- Browser cleanup / expiry / bug
- **Проверка:** Есть ли session cookie при "слёте"

**Сценарий 3: localStorage очищается**
- Storage quota / cleanup / bug
- **Проверка:** Есть ли `server_token` и `tpi` при "слёте"

**Сценарий 4: Приложение сбрасывает состояние**
- API 401/403 response
- Fingerprint validation failed
- **Проверка:** Network tab при следующей авторизации

### Изменения в коде (Третья сессия)

**1. Диагностика localStorage после авторизации**
`services/rpa-service/main.py:3351-3371`

Добавлена проверка localStorage СРАЗУ после RPA steps, до закрытия браузера. Показывает что именно записано в момент успешной авторизации.

**2. Event-based проверка данных перед закрытием**
`services/rpa-service/simple_browser_manager.py:445-471`

Вместо time-based задержки (`sleep(2)`) проверяем что данные присутствуют в localStorage перед `quit()`:
```python
user_id = driver.execute_script("return localStorage.getItem('UserId');")
tpi = driver.execute_script("return localStorage.getItem('tpi');")
if user_id and tpi and server_token:
    logger.info(f"💾 Profile data verified before close: UserId={user_id}, tpi={tpi}")
```

### Команды для мониторинга

**Открыть browse:**
```bash
curl -X POST http://localhost:8004/browse/open \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 99777490, "lsd_name": "perek"}'
```

**Экспорт состояния профиля:**
```bash
python3 /tmp/export_from_profile.py
```

**Проверка через прямое открытие:**
```bash
python3 /tmp/check_auth_direct.py
```

---

## Тест 8: Root Cause Analysis зависания поиска (Order #10)

**Дата:** 19 октября 2025, 22:38-22:43
**Проблема:** Поиск по заказу #10 завис на этапе "Quick wait for page render" и не завершился в течение 5 минут.

### Хронология зависания

```
22:38:30 INFO  ✅ [1/9] ВкусВилл: acquired slot (free slots: 2/3)
22:38:30 INFO  ✅ [2/9] Ozon Fresh: acquired slot (free slots: 1/3)
22:38:30 INFO  ✅ [3/9] Ашан: acquired slot (free slots: 0/3)
22:38:40 INFO  🔓 [3/9] Ашан: releasing slot (found 0 stocks)
22:38:49 INFO  🔓 [1/9] ВкусВилл: releasing slot (found 0 stocks)
22:38:59 INFO  🔍 Starting product search for 5 products on Глобус
22:39:01 INFO  ⏱️ Quick wait for page render (3s before NOT_FOUND check)...
22:39:01 INFO  🔎 [1/5] Processing product: молоко 3.2%  (Samokat)
22:39:01 INFO  ⏱️ Quick wait for page render (3s before NOT_FOUND check)...
22:39:01 INFO  🔎 [2/5] Processing product: хлеб бездрожжевой  (Ozon)
22:39:01 INFO  ⏱️ Quick wait for page render (3s before NOT_FOUND check)...

[ЗАВИСАНИЕ - логи прекратились на 5+ минут]
```

### Root Cause: Event Loop Starvation

**Прямая причина:**
Множественные `await asyncio.sleep(3)` вызовы произошли одновременно в 22:39:01, но ни один не завершился - event loop был заблокирован.

**Коренные причины:**

1. **Перегрузка event loop:**
   - 9 LSD workers запущены одновременно через `asyncio.gather()`
   - Только 3 browser slots доступны (MAX_CONCURRENT_BROWSERS=3)
   - Ozon Fresh занял слот и не освободил его (не завершил поиск)
   - Глобус, Самокат и другие ожидали слоты + выполняли параллельные операции

2. **Отсутствие retry логики:**
   - Таймаут 300 секунд есть (main.py:732), но НЕ срабатывает при зависании event loop
   - При срабатывании таймаута возвращается `[]` (пустой результат) БЕЗ повторной попытки
   - Параметр `MAX_LSD_RETRIES=3` в .env существует, но НЕ используется

3. **Hardcoded timeout:**
   - Таймаут захардкожен в коде: `search_timeout_seconds = 300` (main.py:732)
   - Не настраивается через .env

4. **Блокирующие Selenium операции:**
   - `driver.execute_script()`, `WebDriverWait()` могут блокировать event loop
   - Нет изоляции между async и sync операциями

### Решения

**1. Добавить retry логику (services/rpa-service/main.py:726-755):**
```python
# Используем MAX_LSD_RETRIES из .env
max_retries = settings.max_lsd_retries  # из .env: MAX_LSD_RETRIES=3
retry_count = 0

while retry_count < max_retries:
    try:
        async with browser_semaphore:
            search_results = await asyncio.wait_for(
                perform_product_search_with_cdp_cookies(...),
                timeout=search_timeout_seconds
            )
            break  # Успех - выходим из цикла
    except asyncio.TimeoutError:
        retry_count += 1
        if retry_count < max_retries:
            logger.warning(f"⚠️ Retry {retry_count}/{max_retries} after timeout")
            continue
        else:
            logger.error(f"❌ Max retries reached - returning empty results")
            search_results = []
```

**2. Сделать timeout настраиваемым (.env):**
```bash
# RPA Configuration
RPA_SEARCH_TIMEOUT_SEC=300  # Timeout for product search (5 minutes)
```

**3. Добавить защиту от зависания asyncio.sleep:**
```python
# Вместо:
await asyncio.sleep(3)

# Использовать:
try:
    await asyncio.wait_for(asyncio.sleep(3), timeout=5)
except asyncio.TimeoutError:
    logger.warning("⚠️ Sleep timeout - continuing anyway")
```

**4. Изолировать Selenium операции в executor:**
```python
# Выполнять блокирующие Selenium операции в отдельном потоке
loop = asyncio.get_event_loop()
result = await loop.run_in_executor(
    None,  # default ThreadPoolExecutor
    driver.execute_script,
    "return localStorage.getItem('UserId');"
)
```

### Резюме

- ✅ **Проблема:** Event loop starvation из-за множественных одновременных async операций
- ✅ **Обнаружено:** Зависание на `await asyncio.sleep(3)` в perform_product_search_with_cdp_cookies
- ✅ **Причина:** Ozon Fresh не освободил slot, остальные workers ждали, event loop был перегружен
- ❌ **Retry логика:** Отсутствует (параметр MAX_LSD_RETRIES=3 существует, но не используется)
- ❌ **Timeout:** Hardcoded (300 сек), не настраивается через .env
- 📝 **Статус:** Требуется реализация retry логики и защиты от зависания

## История изменений

- **19.10.2025 02:00** - Первая версия документа (4 теста CDP)
- **19.10.2025 21:30** - Вторая сессия исследования (2 новых теста CDP)
- **19.10.2025 22:20** - Третья сессия: исследование persistent profile (1 тест, 3 гипотезы)
- **19.10.2025 22:43** - Четвёртая сессия: Root Cause Analysis зависания поиска (Order #10)
- **Проведено тестов:** 8
- **Время исследования:** ~4 часа
- **Статус CDP:** ❌ Не работает даже с правильными данными
- **Статус Persistent Profile:** ✅ Работает, но слетает через несколько часов (причина в процессе выяснения)
- **Статус Retry Logic:** ❌ Отсутствует, требуется реализация
- **Следующий шаг:** Реализовать retry логику и защиту от зависания event loop
