# RPA Config Parameters & Actions Reference - Обновлено

## ПОДДЕРЖИВАЕМЫЕ ACTIONS В РАЗДЕЛЕ `delivery_ranges`

Все actions используются в секциях: `trigger`, `navigate`, `close_trigger`.

---

### **1. `action: "click"` - Клик по элементу** ✅

**Описание**: Выполняет клик по элементу.

**Обязательные параметры**:
- `selector` (string) ИЛИ `selectors` (array) - CSS или XPath селектор(ы)

**Опциональные параметры**:
- `wait_after` (number, default: 500) - пауза после клика в миллисекундах

**Fallback**: Если указан массив `selectors`, пробует каждый по очереди до первого успешного.

**Пример**:
```json
{
  "action": "click",
  "selectors": [
    "//button[.//div[normalize-space(.)='За 30 минут']]",
    "//button[.//div[normalize-space(.)='За час']]"
  ],
  "wait_after": 1000
}
```

**Когда использовать**: Открытие модальных окон, нажатие кнопок, выбор опций.

---

### **2. `action: "hover"` - Наведение курсора** ✅

**Описание**: Наводит курсор на элемент (ActionChains).

**Обязательные параметры**:
- `selector` (string) ИЛИ `selectors` (array) - CSS или XPath селектор(ы)

**Опциональные параметры**:
- `wait_after` (number, default: 500) - пауза после hover в миллисекундах

**Fallback**: Поддерживает массив селекторов.

**Пример**:
```json
{
  "action": "hover",
  "selectors": [".menu-delivery", ".delivery-btn"],
  "wait_after": 500
}
```

**Когда использовать**: Открытие выпадающих меню, отображение тултипов.

---

### **3. `action: "type"` - Ввод текста** ✅

**Описание**: Вводит текст в поле. Поддерживает плейсхолдеры для подстановки данных пользователя.

**Обязательные параметры**:
- `selector` (string) ИЛИ `selectors` (array) - CSS или XPath селектор(ы)
- `text` (string) - текст для ввода (поддерживает плейсхолдеры)

**Опциональные параметры**:
- `wait_after` (number, default: 500) - пауза после ввода в миллисекундах

**Fallback**: Поддерживает массив селекторов.

**Поддерживаемые плейсхолдеры**:
- `{phone}` - полный номер (+79161234567)
- `{phone_without_7}` - без кода страны (9161234567)
- `{address}` - полный адрес с индексом (117335, Профсоюзная 32)
- `{zipcode}` - только индекс (117335)
- `{address_wo_zipcode}` - адрес без индекса (Профсоюзная 32)

**Пример**:
```json
{
  "action": "type",
  "selector": "input[name='address']",
  "text": "{address_wo_zipcode}",
  "wait_after": 1000
}
```

**Когда использовать**: Заполнение форм, поиск по адресу.

---

### **4. `action: "wait"` - Ожидание** ✅

**Описание**: Просто ждет указанное время.

**Обязательные параметры**:
- `wait_after` (number) - время ожидания в миллисекундах

**Опциональные параметры**: нет

**Пример**:
```json
{
  "action": "wait",
  "wait_after": 2000
}
```

**Когда использовать**: Ожидание загрузки, паузы между действиями.

---

### **5. `action: "navigate"` - Переход на URL** ✅ НОВЫЙ

**Описание**: Выполняет переход на указанный URL.

**Обязательные параметры**:
- `url` (string) - полный URL для перехода

**Опциональные параметры**:
- `wait_after` (number, default: 500) - пауза после навигации в миллисекундах

**Пример**:
```json
{
  "action": "navigate",
  "url": "https://www.ozon.ru/cart",
  "wait_after": 3000
}
```

**Когда использовать**: Переход на другую страницу (корзина, checkout), смена раздела сайта.

---

## 📊 СТРУКТУРА КОНФИГА `delivery_ranges`

### **Полная структура** (обновлено):

```json
{
  "enabled": true,
  
  "trigger": {
    "action": "click|hover|type|wait",
    "selector": "CSS или XPath",
    "selectors": ["селектор1", "селектор2"],
    "text": "текст для type action",
    "wait_after": 1000
  },
  
  "navigate": {
    "action": "navigate|click",
    "url": "https://domain.com/cart",
    "selectors": ["//a[@href='/cart']"],
    "wait_after": 3000
  },
  
  "ordered_extraction": {
    "fee_selectors": [
      "//span[@class='fee-1']",
      "//span[@class='fee-2']",
      null
    ],
    "threshold_selectors": [
      "//span[@class='threshold-1']",
      "//span[@class='threshold-2']",
      "//span[@class='threshold-3']"
    ]
  },
  
  "min_order_amount_selector": "//span[contains(., 'минимальный заказ')]",
  
  "close_trigger": {
    "selector": ".modal-close",
    "optional": true
  }
}
```

---

## 🔧 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### **Пример 1: Простой trigger + navigate**

```json
{
  "delivery_ranges": {
    "enabled": true,
    
    "trigger": {
      "action": "click",
      "selectors": [
        "//button[.//div[normalize-space(.)='За 30 минут']]",
        "//button[.//div[normalize-space(.)='За час']]"
      ],
      "wait_after": 1000
    },
    
    "navigate": {
      "action": "navigate",
      "url": "https://www.ozon.ru/cart",
      "wait_after": 3000
    },
    
    "min_order_amount_selector": "//span[contains(., 'Доставка Ozon fresh')]"
  }
}
```

### **Пример 2: Navigate через клик**

```json
{
  "delivery_ranges": {
    "enabled": true,
    
    "trigger": {
      "action": "hover",
      "selector": ".delivery-menu"
    },
    
    "navigate": {
      "action": "click",
      "selectors": [
        "//a[@href='/cart']",
        "//button[text()='Корзина']"
      ],
      "wait_after": 2000
    }
  }
}
```

### **Пример 3: Без navigate (извлечение на текущей странице)**

```json
{
  "delivery_ranges": {
    "enabled": true,
    
    "trigger": {
      "action": "click",
      "selector": ".delivery-info-btn"
    },
    
    "ordered_extraction": {
      "fee_selectors": ["//span[@class='fee-1']", null],
      "threshold_selectors": ["//span[@class='t-1']", "//span[@class='t-2']"]
    }
  }
}
```

---

## 🎯 ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Селекторы:** Всегда используйте массив `selectors` для fallback (кроме navigate)
2. **XPath:** Начинается с `//` или `(//`
3. **CSS:** Все остальные селекторы считаются CSS
4. **Таймауты:** Указываются в миллисекундах
5. **Плейсхолдеры:** Работают только в `action: "type"`
6. **Navigate:** НЕ требует селекторов, только `url`
7. **Секция `navigate`:** Опциональная, выполняется после `trigger`

---

## 📋 ЛОГИКА ВЫПОЛНЕНИЯ `delivery_ranges`

```
1. Выполнить trigger (открыть модальное окно / навести курсор)
   ├─ Если есть массив actions → выполнить последовательно
   └─ Если есть selectors → fallback логика (пробовать по очереди)

2. Выполнить navigate (если есть)
   ├─ action: "navigate" → driver.get(url)
   └─ action: "click" → клик по селектору для перехода

3. Извлечь min_order_amount (если есть селектор)

4. Извлечь диапазоны доставки через ordered_extraction

5. Закрыть модальное окно (если есть close_trigger)

6. Вернуть (ranges_data, min_order_amount)
```

---

## ✅ СТАТУС РЕАЛИЗАЦИИ

| Action | Статус | Fallback селекторов | Примечания |
|--------|--------|---------------------|------------|
| `click` | ✅ | ✅ | Поддержка массива selectors |
| `hover` | ✅ | ✅ | ActionChains |
| `type` | ✅ | ✅ | Плейсхолдеры + fallback |
| `wait` | ✅ | N/A | Селекторы не нужны |
| `navigate` | ✅ | N/A | Только URL, селекторы не нужны |

---

## 🚀 ОБНОВЛЕНО В ТЕКУЩЕЙ ВЕРСИИ

1. ✅ Добавлена поддержка `action: "navigate"`
2. ✅ Добавлена секция `navigate` в `delivery_ranges`
3. ✅ Добавлена fallback логика для массивов селекторов во всех actions
4. ✅ Исправлен баг с return в `extract_delivery_ranges`
5. ✅ Переименовано `open_cart` → `navigate`

---

**Дата обновления:** 2025-10-06  
**Версия:** 2.0
